use anyhow::Result;
use std::path::Path;

pub fn run(_config_path: Option<&Path>) -> Result<()> {
    println!("macup diff - Not yet implemented");
    println!("This will show the difference between config and current system state");
    Ok(())
}
